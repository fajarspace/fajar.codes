-- ============================================================================
-- fajar.codes — remove the timeline feature
-- The site no longer shows a timeline anywhere, so the table goes too.
-- ============================================================================
drop table if exists public.timeline;
